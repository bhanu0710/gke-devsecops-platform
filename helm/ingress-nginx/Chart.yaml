apiVersion: v2
name: ingress-nginx
description: Nginx Ingress Controller — NodePort only (no GCP HTTP LB cost)
type: application
version: 1.0.0
dependencies:
  - name: ingress-nginx
    version: "4.10.1"
    repository: "https://kubernetes.github.io/ingress-nginx"
