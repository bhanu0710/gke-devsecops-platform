replicaCount: 1 # Lower replica count saves cost in staging
image:
  tag: "6c1e087e"
resources:
  requests:
    cpu: "50m"
    memory: "64Mi"
  limits:
    cpu: "250m"
    memory: "128Mi"
autoscaling:
  minReplicas: 1
  maxReplicas: 3

ingress:
  enabled: true
  paths:
    - path: /users
