# Production overrides — merged on top of values.yaml by ArgoCD
replicaCount: 1   # keep low on free-tier spot cluster (limited CPU)

resources:
  requests:
    cpu: "50m"
    memory: "128Mi"
  limits:
    cpu: "300m"
    memory: "256Mi"
